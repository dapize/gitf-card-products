export interface IProduct {
  code: string;
  name: string;
  image: string;
  slug: string;
  normalPrice: number;
  internetPrice: number;
  cardPrice: number;
  category: string;
  subCategory: string;
}
